# Task Management System (ASP.NET Core + Vue)

Тестовое приложение для управления задачами с ролевой моделью.

## Быстрый старт

### Предварительные требования
- [.NET 8 SDK](https://dotnet.microsoft.com/download)
- [Node.js 18+](https://nodejs.org/)
- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (опционально)

---

## Способ 1: Запуск через Docker (рекомендуется)

```bash
# Клонируем репозиторий
git clone https://github.com/blessed234640/TaskManagementSystem2.git
cd TaskManagementSystem2

# Запускаем backend в Docker
cd TaskManagement.Api
docker-compose up -d

# Backend доступен по адресу: http://localhost:8080
# Swagger: http://localhost:8080/swagger

# В отдельном терминале запускаем frontend
cd frontend
npm install
npm run dev
# Frontend: http://localhost:5173
```

---

## Способ 2: Локальный запуск (для разработки)

### Терминал 1 - Backend:

```bash
cd TaskManagementSystem2/TaskManagement.Api
dotnet restore
dotnet run --urls "https://localhost:7073"
# API: https://localhost:7073
# Swagger: https://localhost:7073/swagger
```

### Терминал 2 - Frontend:

```bash
cd TaskManagementSystem2/frontend
npm install
npm run dev
# Frontend: http://localhost:5173
```

⚠️ При первом запуске браузер может ругнуться на SSL сертификат. 
Откройте https://localhost:7073 и примите предупреждение.

---

## Работа со Swagger UI

### Правильная авторизация в Swagger

**Шаг 1: Получение JWT токена**
1. Откройте Swagger: `http://localhost:8080/swagger` (Docker) или `https://localhost:7073/swagger` (локально)
2. Найдите эндпоинт `POST /api/Auth/login`
3. Нажмите кнопку **"Try it out"**
4. Введите данные для авторизации:
   ```json
   {
     "email": "manager@example.com",
     "password": "manager123"
   }
   ```
5. Нажмите **"Execute"**
6. **Скопируйте токен** из поля "token" в ответе (весь токен целиком)

**Шаг 2: Активация JWT авторизации**
1. Нажмите кнопку **"Authorize"** вверху справа (рядом с кнопками "Execute"/"Clear")
2. В появившемся окне введите: `Bearer ваш_скопированный_токен`
   - **ВАЖНО**: Обязательно начните с `Bearer ` (через пробел)
   - Пример: `Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
3. Нажмите кнопку **"Authorize"**

**Шаг 3: Проверка авторизации**
1. Закройте окно авторизации (нажмите "Close")
2. Теперь у всех защищенных эндпоинтов появится **замок 🔒** 
3. Попробуйте выполнить `GET /api/Auth/me` - должен вернуть данные пользователя

**Шаг 4: Тестирование API**
- Теперь все запросы автоматически включают заголовок `Authorization: Bearer токен`
- Все защищенные эндпоинты должны работать
- Токен действует 120 минут

### Частые ошибки и решения

| Проблема | Решение |
|----------|----------|
| "401 Unauthorized" | Убедитесь, что ввели `Bearer токен` (с префиксом Bearer через пробел) |
| "Token expired" | Получите новый токен через `POST /api/Auth/login` |
| "No Authorization header" | Нажмите кнопку "Authorize" и введите токен с префиксом `Bearer ` |
| "Invalid token format" | Убедитесь, что скопировали токен полностью без лишних символов |

### Результат успешной настройки
- ✅ Все эндпоинты показывают замок 🔒
- ✅ Запросы успешно выполняются
- ✅ В ответах приходят корректные данные
- ✅ Frontend также работает с теми же учетными данными

---

## Ролевая модель

| Роль | Права |
|-------|--------|
| MANAGER (Начальник) | Создание, редактирование, назначение исполнителя, удаление любых задач |
| EMPLOYEE (Сотрудник) | Редактирование и изменение статуса только своих задач |
| VIEWER (Наблюдатель) | Только просмотр задач |

---

## Тестовые учетные записи

После первого запуска база данных автоматически заполняется тестовыми пользователями:

| Роль | Email | Пароль |
|-------|--------|---------|
| Начальник | manager@example.com | manager123 |
| Сотрудник | employee@example.com | employee123 |
| Наблюдатель | viewer@example.com | viewer123 |

---

## API Документация

После запуска доступна Swagger документация:

Локально: https://localhost:7073/swagger
В Docker: http://localhost:8080/swagger

### Основные эндпоинты:
- POST /api/Auth/login - получение JWT токена
- GET /api/Tasks - список задач (с фильтрацией)
- POST /api/Tasks - создание задачи (только MANAGER)
- PUT /api/Tasks/{id}/status - изменение статуса
- POST /api/Tasks/{id}/assign - назначение исполнителя

---

## Архитектура проекта

TaskManagementSystem2/
├── TaskManagement.Api/
│   ├── Domain/               # Сущности и бизнес-логика
│   │   ├── Entities/         # User, Task, Role, и т.д.
│   │   └── Enums/            # TaskStatus, TaskPriority
│   ├── Infrastructure/       # Работа с БД
│   │   ├── AppDbContext.cs   # EF Core контекст
│   │   └── DataSeeder.cs     # Начальные данные
│   ├── Auth/                 # JWT аутентификация
│   ├── Services/             # Бизнес-слой
│   ├── Controllers/          # API эндпоинты
│   └── Transport/            # DTO для запросов/ответов

frontend/                 # Vue 3 приложение
├── src/
│   ├── App.vue          # Основной компонент
│   └── main.js          # Точка входа
└── index.html

---

## Переменные окружения

Backend (appsettings.json или переменные):
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=tasks.db"  # или для Docker: "/app/data/tasks.db"
  },
  "JwtSettings": {
    "Secret": "your-secret-key-min-16-characters",
    "ExpirationHours": 24
  }
}

Frontend (.env файл):
VITE_API_URL=https://localhost:7073  # для локального API
# или
VITE_API_URL=http://localhost:8080   # для API в Docker

---

## Частые проблемы и решения

1. "Не удается подключиться к API"
Проверьте, что backend запущен
Убедитесь, что порты совпадают (7073 или 8080)
Для Docker: docker ps должен показывать запущенный контейнер

2. "Ошибка CORS"
В Development режиме CORS уже настроен
При деплое накатите миграции: dotnet ef database update

3. База данных не создается
cd TaskManagement.Api
dotnet ef database update

---

## Технологии

Backend: .NET 8, EF Core, SQLite, JWT
Frontend: Vue 3, Vite, Fetch API
Контейнеризация: Docker, docker-compose
## 📚 API Документация

После запуска доступна Swagger документация:
- Локально: https://localhost:7073/swagger
- В Docker: http://localhost:8080/swagger

### Основные эндпоинты:
- `POST /api/auth/login` - получение JWT токена
- `GET /api/tasks` - список задач (с фильтрацией)
- `POST /api/tasks` - создание задачи (только MANAGER)
- `PUT /api/tasks/{id}/status` - изменение статуса
- `POST /api/tasks/{id}/assign` - назначение исполнителя

## 🏗 Архитектура проекта

```
TaskManagement.Api/
├── Domain/               # Сущности и бизнес-логика
│   ├── Entities/         # User, Task, Role, и т.д.
│   └── Enums/            # TaskStatus, TaskPriority
├── Infrastructure/       # Работа с БД
│   ├── AppDbContext.cs   # EF Core контекст
│   └── DataSeeder.cs     # Начальные данные
├── Auth/                 # JWT аутентификация
├── Services/             # Бизнес-слой
├── Controllers/          # API эндпоинты
└── Transport/            # DTO для запросов/ответов

frontend/                 # Vue 3 приложение
├── src/
│   ├── App.vue          # Основной компонент
│   └── main.js          # Точка входа
└── index.html
```

## 🔧 Переменные окружения

### Backend (appsettings.json или переменные):
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=tasks.db"  // или для Docker: "/app/data/tasks.db"
  },
  "JwtSettings": {
    "Secret": "your-secret-key-min-16-characters",
    "ExpirationHours": 24
  }
}
```

### Frontend (.env файл):
```env
VITE_API_URL=https://localhost:7073  # для локального API
# или
VITE_API_URL=http://localhost:8080   # для API в Docker
```

## ❗ Частые проблемы и решения

### 1. "Не удается подключиться к API"
- Проверьте, что backend запущен
- Убедитесь, что порты совпадают (7073 или 8080)
- Для Docker: `docker ps` должен показывать запущенный контейнер

### 2. "Ошибка CORS"
- В Development режиме CORS уже настроен
- При деплое накатите миграции: `dotnet ef database update`

### 3. База данных не создается
```bash
cd TaskManagement.Api
dotnet ef database update
```

## 📦 Технологии

- **Backend**: .NET 8, EF Core, SQLite, JWT
- **Frontend**: Vue 3, Vite, Fetch API
- **Контейнеризация**: Docker, docker-compose
>>>>>>> 4373a50c5ccf945e2b1ec7fd2b713c8413dde82b
